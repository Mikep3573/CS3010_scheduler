#include "scheduler.mpiscion.h"

// Define the algorithm for SJF and FCFS
void scheduler() {
    // Create tasks and put them into the taskQueue
    // Set currentTime = 0
    // Set totalWaitTime = 0
    // While not done
    //  If taskQueue is empty
    //   Set done = true
    //  else
    //   dequeue task from taskQueue
    //   task.totalWaitTime = task.totalWaitTime + (currentTime - task.submitTime)
    //   totalWaitTime = totalWaitTime + task.totalWaitTime
    //   currentTime = currentTime + task.totalBurstTime
}